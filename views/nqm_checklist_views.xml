<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_nqm_checklist_form" model="ir.ui.view">
        <field name="name">nqm.checklist.form</field>
        <field name="model">nqm.checklist</field>
        <field name="arch" type="xml">
            <form string="Checklist réseau">
                <header>
                    <button name="action_mark_done" string="✅ Tout valider" type="object"
                            class="btn-success" invisible="state != 'open'"/>
                    <button name="action_reopen" string="Rouvrir" type="object"
                            invisible="state != 'done'"/>
                    <field name="state" widget="statusbar" statusbar_visible="open,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Titre de la checklist..."/></h1>
                        <field name="ref" readonly="1" class="oe_tag"/>
                    </div>

                    <group>
                        <group>
                            <field name="project_id"/>
                            <field name="phase"/>
                            <field name="assigned_to"/>
                            <field name="due_date"/>
                        </group>
                        <group>
                            <field name="is_template"/>
                            <field name="total_items"/>
                            <field name="done_items"/>
                            <field name="completion_rate" widget="progressbar"/>
                        </group>
                    </group>
                    <field name="notes" placeholder="Contexte ou notes supplémentaires..."/>

                    <separator string="Points de contrôle"/>
                    <field name="item_ids">
                        <list editable="bottom" decoration-success="state == 'done'"
                              decoration-danger="state == 'failed'"
                              decoration-muted="state == 'na'">
                            <field name="sequence" widget="handle"/>
                            <field name="category"/>
                            <field name="name"/>
                            <field name="is_critical"/>
                            <field name="assigned_to" widget="many2one_avatar_user"/>
                            <field name="state" widget="badge"
                                   decoration-success="state == 'done'"
                                   decoration-danger="state == 'failed'"
                                   decoration-info="state == 'in_progress'"
                                   decoration-muted="state == 'na'"/>
                            <field name="done_date"/>
                            <field name="notes" optional="show"/>
                        </list>
                        <form>
                            <group>
                                <field name="name"/>
                                <field name="category"/>
                                <field name="is_critical"/>
                                <field name="state"/>
                                <field name="assigned_to"/>
                                <field name="done_date"/>
                            </group>
                            <field name="description" placeholder="Critère de validation détaillé..."/>
                            <field name="notes" placeholder="Remarques, numéro de ticket, preuves..."/>
                            <group string="Preuve / Capture">
                                <field name="evidence_attachment" widget="binary" filename="evidence_filename"/>
                                <field name="evidence_filename"/>
                            </group>
                        </form>
                    </field>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_nqm_checklist_list" model="ir.ui.view">
        <field name="name">nqm.checklist.list</field>
        <field name="model">nqm.checklist</field>
        <field name="arch" type="xml">
            <list string="Checklists" decoration-success="state == 'done'"
                  decoration-muted="state == 'cancelled'">
                <field name="ref"/>
                <field name="name"/>
                <field name="project_id"/>
                <field name="phase"/>
                <field name="assigned_to" widget="many2one_avatar_user"/>
                <field name="due_date"/>
                <field name="total_items"/>
                <field name="done_items"/>
                <field name="completion_rate" widget="progressbar"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'done'"
                       decoration-info="state == 'open'"
                       decoration-muted="state == 'cancelled'"/>
            </list>
        </field>
    </record>

    <record id="view_nqm_checklist_search" model="ir.ui.view">
        <field name="name">nqm.checklist.search</field>
        <field name="model">nqm.checklist</field>
        <field name="arch" type="xml">
            <search string="Rechercher une checklist">
                <field name="name"/>
                <field name="project_id"/>
                <separator/>
                <filter string="En cours" name="open" domain="[('state', '=', 'open')]"/>
                <filter string="Terminées" name="done" domain="[('state', '=', 'done')]"/>
                <separator/>
                <group expand="0" string="Grouper par">
                    <filter string="Projet" name="group_project" context="{'group_by': 'project_id'}"/>
                    <filter string="Phase" name="group_phase" context="{'group_by': 'phase'}"/>
                    <filter string="Assigné à" name="group_user" context="{'group_by': 'assigned_to'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_nqm_checklist" model="ir.actions.act_window">
        <field name="name">Checklists</field>
        <field name="res_model">nqm.checklist</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_nqm_checklist_search"/>
    </record>

</odoo>
